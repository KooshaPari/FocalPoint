[package]
name = "pheno-otel"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Stub pheno-otel crate exposing OtelError, TelemetryGuard, init, and Span for observability backends"
rust-version = "1.75"

[dependencies]
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"] }

[lib]
name = "pheno_otel"
path = "src/lib.rs"
