import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Testing Strategy","description":"FocalPoint documentation","frontmatter":{"title":"Testing Strategy","description":"FocalPoint documentation"},"headers":[],"relativePath":"architecture/testing_strategy.md","filePath":"architecture/testing_strategy.md","lastUpdated":1777005079000}');
const _sfc_main = { name: "architecture/testing_strategy.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="testing-strategy" tabindex="-1">Testing Strategy <a class="header-anchor" href="#testing-strategy" aria-label="Permalink to &quot;Testing Strategy&quot;">​</a></h1><p>FocalPoint employs a three-layer testing approach, each targeting a different boundary of the cross-platform Rust↔iOS integration.</p><h2 id="test-layers" tabindex="-1">Test Layers <a class="header-anchor" href="#test-layers" aria-label="Permalink to &quot;Test Layers&quot;">​</a></h2><h3 id="layer-1-rust-unit-tests" tabindex="-1">Layer 1: Rust Unit Tests <a class="header-anchor" href="#layer-1-rust-unit-tests" aria-label="Permalink to &quot;Layer 1: Rust Unit Tests&quot;">​</a></h3><p><strong>Scope:</strong> Core domain logic, isolated subsystems.</p><p><strong>Invocation:</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cargo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> test</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --workspace</span></span></code></pre></div><p><strong>Coverage:</strong></p><ul><li>Event sourcing (append-only log + tamper detection)</li><li>Rule evaluation (condition matching, action dispatch)</li><li>Wallet mutations (credit grant, spend, multiplier)</li><li>Penalty tier escalation</li><li>Task lifecycle state machines</li><li>Audit chain verification (SHA-256 hash chains)</li></ul><p><strong>Hermetic:</strong> Yes. Uses in-memory stores (InMemoryAuditStore, InMemoryCalendarPort) + seeded fixtures. No network.</p><h3 id="layer-2-ios-snapshot-tests" tabindex="-1">Layer 2: iOS Snapshot Tests <a class="header-anchor" href="#layer-2-ios-snapshot-tests" aria-label="Permalink to &quot;Layer 2: iOS Snapshot Tests&quot;">​</a></h3><p><strong>Scope:</strong> UI component rendering, design system consistency.</p><p><strong>Invocation:</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> apps/ios/FocalPoint</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">fastlane</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> snapshot</span></span></code></pre></div><p><strong>Coverage:</strong></p><ul><li>Enforcement view hierarchies</li><li>Mascot UI state transitions</li><li>Design system components (palette, typography, layout)</li><li>Onboarding flow screens</li><li>Diagnostics UI</li></ul><p><strong>Hermetic:</strong> Yes. Uses recorded snapshot images + synthetic mock data. No network, no real app state.</p><p><strong>First Run:</strong> <code>RECORD=true fastlane snapshot</code> to record baseline snapshots. Add snapshots to version control.</p><h3 id="layer-3-ios-integration-tests" tabindex="-1">Layer 3: iOS Integration Tests <a class="header-anchor" href="#layer-3-ios-integration-tests" aria-label="Permalink to &quot;Layer 3: iOS Integration Tests&quot;">​</a></h3><p><strong>Scope:</strong> End-to-end Rust↔Swift FFI bridge; event pipeline; state mutations.</p><p><strong>Invocation:</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> apps/ios/FocalPoint</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">fastlane</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> integration</span></span></code></pre></div><p><strong>Coverage:</strong></p><ul><li>Host event injection → rule evaluation → wallet credit → audit record</li><li>Task CRUD operations + audit trail</li><li>Audit chain tamper detection (SHA-256 verification)</li><li>Connector registration (Canvas, Slack, GitHub)</li><li>Simlish phoneme mapping (MascotUI voice synthesis)</li></ul><p><strong>Test Target:</strong> <code>FocalPointIntegrationTests</code> (XCTest bundle)</p><p><strong>Hermetic:</strong> Yes. Uses temporary SQLite databases (created in FileManager tmpdir, cleaned up post-test). Sets env vars for secret store (memory-backed). No network (HTTP injection deferred to Phase 2).</p><p><strong>Tests:</strong></p><ol><li><p><strong>test_focus_session_credits_wallet</strong></p><ul><li>Traces to: FR-REWARD-001, FR-AUDIT-001</li><li>Creates core, installs <code>deep-work-starter</code> template, emits <code>focus:session_started</code> event, ticks sync + eval, verifies rule.fired audit record, asserts wallet balance &gt; 0.</li></ul></li><li><p><strong>test_task_lifecycle</strong></p><ul><li>Traces to: FR-PLAN-001, FR-AUDIT-001</li><li>Adds task, lists, marks done, removes; verifies task.created, task.status_changed, task.deleted audit records at each step.</li></ul></li><li><p><strong>test_audit_chain_verify</strong></p><ul><li>Traces to: FR-AUDIT-002</li><li>Performs 10 mutations, verifyChain() → true; tampering via direct SQLite UPDATE, verifyChain() → false.</li></ul></li><li><p><strong>test_connector_registration_after_connect</strong></p><ul><li>Traces to: FR-SYNC-001</li><li>Sets env vars (FOCALPOINT_CANVAS_CLIENT_ID, etc.), calls connectCanvas() with fake credentials, asserts sync().connectors() contains &quot;canvas&quot;.</li><li><strong>Skipped on Phase 1:</strong> FFI HTTP client injection not yet available. Deferred to Phase 2.</li></ul></li><li><p><strong>test_simlish_phoneme_mapping</strong></p><ul><li>Traces to: FR-MASCOT-002</li><li>Verifies SimlishVoice helper deterministically maps input text to phoneme sequences.</li></ul></li></ol><h2 id="ci-cd-integration" tabindex="-1">CI/CD Integration <a class="header-anchor" href="#ci-cd-integration" aria-label="Permalink to &quot;CI/CD Integration&quot;">​</a></h2><h3 id="local-development" tabindex="-1">Local Development <a class="header-anchor" href="#local-development" aria-label="Permalink to &quot;Local Development&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># All three layers</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cargo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> test</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --workspace</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> apps/ios/FocalPoint</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">fastlane</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> snapshot</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">fastlane</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> integration</span></span></code></pre></div><h3 id="github-actions-future" tabindex="-1">GitHub Actions (Future) <a class="header-anchor" href="#github-actions-future" aria-label="Permalink to &quot;GitHub Actions (Future)&quot;">​</a></h3><p>Once GitHub Actions billing is resolved:</p><ul><li>Rust unit tests on Linux runner (free tier) — blocks PR merge.</li><li>iOS snapshot tests on macOS runner (billed) — informational; does not block.</li><li>iOS integration tests on macOS runner (billed) — informational; does not block.</li></ul><h3 id="quality-gates" tabindex="-1">Quality Gates <a class="header-anchor" href="#quality-gates" aria-label="Permalink to &quot;Quality Gates&quot;">​</a></h3><ul><li><strong>Pre-commit:</strong> Rust clippy + fmt + test (local only; no shell escape).</li><li><strong>Branch protection:</strong> Rust unit tests must pass. iOS tests are informational.</li></ul><h2 id="skipped-tests-rationale" tabindex="-1">Skipped Tests &amp; Rationale <a class="header-anchor" href="#skipped-tests-rationale" aria-label="Permalink to &quot;Skipped Tests &amp; Rationale&quot;">​</a></h2><h3 id="test-connector-registration-after-connect-phase-2" tabindex="-1">test_connector_registration_after_connect (Phase 2) <a class="header-anchor" href="#test-connector-registration-after-connect-phase-2" aria-label="Permalink to &quot;test_connector_registration_after_connect (Phase 2)&quot;">​</a></h3><p>The current FFI surface does not expose HTTP client injection for testing. The Canvas connector integration requires mocking the OAuth callback + HTTP exchange. Deferral allows Phase 1 to ship without external HTTP mocking infrastructure.</p><p><strong>Unblock:</strong> Implement <code>HttpClientPort</code> trait, expose injectable HTTP layer in FFI.</p><h3 id="test-audit-chain-verify-sqlite-tamper-partial" tabindex="-1">test_audit_chain_verify SQLite Tamper (Partial) <a class="header-anchor" href="#test-audit-chain-verify-sqlite-tamper-partial" aria-label="Permalink to &quot;test_audit_chain_verify SQLite Tamper (Partial)&quot;">​</a></h3><p>Direct SQLite access for tamper simulation requires either:</p><ul><li>GRDB (Objective-C++ / Swift package) — adds dependency.</li><li>sqlite3 C API — requires Bridging-Header.</li></ul><p>Current implementation documents the intent; the test skips gracefully if SQLite direct access is unavailable (sandbox restriction or missing helper). Full implementation deferred to when a test SQLite helper is available.</p><h2 id="performance-expectations" tabindex="-1">Performance Expectations <a class="header-anchor" href="#performance-expectations" aria-label="Permalink to &quot;Performance Expectations&quot;">​</a></h2><ul><li><strong>Rust unit tests:</strong> ~30–60 seconds (full workspace).</li><li><strong>iOS snapshots:</strong> ~2–5 minutes (per-device variant).</li><li><strong>iOS integration tests:</strong> ~1–2 minutes (tempdir setup + 5 scenarios).</li></ul><h2 id="future-enhancements" tabindex="-1">Future Enhancements <a class="header-anchor" href="#future-enhancements" aria-label="Permalink to &quot;Future Enhancements&quot;">​</a></h2><ol><li><strong>Deterministic Clock Injection:</strong> Allow tests to inject a fixed SystemTime instead of wallclock; reduces flakiness in rule scheduling tests.</li><li><strong>HTTP Mock Layer:</strong> Expose HttpClientPort in FFI; unblock connector integration tests.</li><li><strong>Snapshot Diffing:</strong> Use <code>swift-snapshot-testing</code> visual diffs in CI to detect regressions.</li><li><strong>Load Testing:</strong> Parameterized tests to stress-test audit chain hashing + wallet mutations at scale (10K+ records).</li><li><strong>Accessibility Testing:</strong> XCUITest automation for accessibility audit (VoiceOver, Dynamic Type).</li></ol><h2 id="test-traceability" tabindex="-1">Test Traceability <a class="header-anchor" href="#test-traceability" aria-label="Permalink to &quot;Test Traceability&quot;">​</a></h2><p>Every test is traced to a Functional Requirement (FR) in the spec:</p><div class="language-swift vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">swift</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// Traces to: FR-REWARD-001, FR-AUDIT-001</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">func</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> testFocusSessionCreditsWallet</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">throws</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">...</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span></code></pre></div><p>Run the traceability audit:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">grep</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -r</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;Traces to:&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Tests/</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> sort</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> uniq</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -c</span></span></code></pre></div><p>All FRs must have ≥1 test; all tests must reference ≥1 FR.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("architecture/testing_strategy.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const testing_strategy = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  testing_strategy as default
};
